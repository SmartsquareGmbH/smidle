apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ingress.yaml
  - smidle-deployment.yaml
  - smidle-service.yaml
secretGenerator:
  - name: docker-registries
    files:
      - .dockerconfigjson=docker-registry-auth.json
    type: kubernetes.io/dockerconfigjson
  - name: smidle-secret
    literals:
      - postgres-password=tuuado69z4ynd05t
configMapGenerator:
  - name: backendconfig
    files:
      - rest-api.properties
      - ca-certificate-postgresql.crt
